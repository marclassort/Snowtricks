{% extends 'base.html.twig' %}

{% block title %}
	Éditer une figure{{ parent() }}
{% endblock %}

{% block body %}
	<div class="row mt-5">
		<div class="col-md-6 mx-auto">
			<div class="container">
				<h1>Éditer la figure
					{{ trick.name }}</h1>
				<div class="mt-5">
					{{ include('figures/_form.html.twig')}}
				</div>
			</div>
		</div>
	</div>
{% endblock %}

{% block javascripts %}
	{{ parent() }}
{% endblock %}
