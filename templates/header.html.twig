<nav class="navbar navbar-expand-lg navbar-light bg-light rounded" aria-label="navbar">
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('home') }}">
			<div class="container" style="width: 200px;">
				<img src="/img/logo-snowtricks.png" alt="logo snowtricks" style="width: 100%">
			</div>
		</a>
		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbar" aria-controls="navbar-example" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="navbar">
			<ul class="navbar-nav ms-auto mb-2 mb-lg-0">
				<li class="nav-item ml-3">
					<a class="nav-link active" aria-current="page" href="{{ path('home') }}">Accueil</a>
				</li>
				<li class="nav-item ml-3">
					<a class="nav-link" href="{{ path('home') }}#tricks">Figures</a>
				</li>
				{% if app.user is null %}
					<li class="nav-item pl-3">
						<a class="nav-link" href="{{ path('app_login') }}">Login</a>
					</li>
					<li class="nav-item ml-3">
						<a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
					</li>
				{% endif %}
				{% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
					<li class="nav-item ml-3">
						<a class="nav-link" href="{{ path('app_logout') }}">Déconnexion</a>
					</li>
				{% endif %}
			</ul>
		</div>
	</div>
</nav>
